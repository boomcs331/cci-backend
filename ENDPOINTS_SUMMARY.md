# 🚀 Complete API Endpoints Summary

## 📋 Overview
ระบบ Authentication & Authorization ที่สมบูรณ์แบบด้วย NestJS + TypeORM รองรับ Role-Based Access Control (RBAC) พร้อม 32 endpoints

**Base URL:** `http://localhost:3000`

---

## 🔐 Authentication Endpoints (2)

### 1. สมัครสมาชิก
```http
POST /auth/register
```
**วัตถุประสงค์:** สร้างผู้ใช้ใหม่ในระบบ  
**Body:** `{ username, email, password, firstName?, lastName?, roleIds? }`  
**Response:** ข้อมูลผู้ใช้ใหม่ (ไม่รวม password)

### 2. เข้าสู่ระบบ
```http
POST /auth/login
```
**วัตถุประสงค์:** ตรวจสอบ username + password และส่งคืนข้อมูลผู้ใช้พร้อมสิทธิ์  
**Body:** `{ username, password }`  
**Response:** ข้อมูลผู้ใช้ + รายการสิทธิ์ทั้งหมด

---

## 👥 User Management Endpoints (6)

### 3. ดูรายการผู้ใช้ทั้งหมด
```http
GET /auth/users
```
**วัตถุประสงค์:** ดึงรายการผู้ใช้ทั้งหมดพร้อมบทบาท  
**Response:** Array ของผู้ใช้ (ไม่รวม password)

### 4. ดูข้อมูลผู้ใช้เฉพาะ
```http
GET /auth/users/:id
```
**วัตถุประสงค์:** ดูรายละเอียดผู้ใช้คนเดียวพร้อมบทบาทและสิทธิ์  
**Params:** `id` - User ID  
**Response:** ข้อมูลผู้ใช้ครบถ้วน

### 5. แก้ไขข้อมูลผู้ใช้
```http
PUT /auth/users/:id
```
**วัตถุประสงค์:** อัปเดตข้อมูลผู้ใช้และบทบาท  
**Params:** `id` - User ID  
**Body:** `{ username?, email?, firstName?, lastName?, isActive?, roleIds? }`  
**Response:** ข้อมูลผู้ใช้ที่อัปเดตแล้ว

### 6. ลบผู้ใช้
```http
DELETE /auth/users/:id
```
**วัตถุประสงค์:** ลบผู้ใช้ออกจากระบบถาวร  
**Params:** `id` - User ID  
**Response:** ข้อความยืนยันการลบ

### 7. เปิด/ปิดสถานะผู้ใช้
```http
PATCH /auth/users/:id/toggle-status
```
**วัตถุประสงค์:** เปลี่ยนสถานะ `isActive` ของผู้ใช้  
**Params:** `id` - User ID  
**Response:** ข้อมูลผู้ใช้ที่อัปเดตสถานะแล้ว

### 8. ดูสิทธิ์ของผู้ใช้
```http
GET /auth/users/:id/permissions
```
**วัตถุประสงค์:** ดูรายการสิทธิ์ทั้งหมดของผู้ใช้ (จากบทบาทที่มี)  
**Params:** `id` - User ID  
**Response:** Array ของ permission codes

---

## 🎭 Role Management Endpoints (6)

### 9. ดูรายการบทบาททั้งหมด
```http
GET /auth/roles
```
**วัตถุประสงค์:** ดึงรายการบทบาททั้งหมดพร้อมสิทธิ์  
**Response:** Array ของบทบาท

### 10. สร้างบทบาทใหม่
```http
POST /auth/roles
```
**วัตถุประสงค์:** สร้างบทบาทใหม่พร้อมกำหนดสิทธิ์  
**Body:** `{ code, name, description?, isSystem?, permissionIds? }`  
**Response:** ข้อมูลบทบาทใหม่

### 11. ดูข้อมูลบทบาทเฉพาะ
```http
GET /auth/roles/:id
```
**วัตถุประสงค์:** ดูรายละเอียดบทบาทพร้อมสิทธิ์และผู้ใช้  
**Params:** `id` - Role ID  
**Response:** ข้อมูลบทบาทครบถ้วน

### 12. แก้ไขข้อมูลบทบาท
```http
PUT /auth/roles/:id
```
**วัตถุประสงค์:** อัปเดตข้อมูลบทบาทและสิทธิ์  
**Params:** `id` - Role ID  
**Body:** `{ code?, name?, description?, isSystem?, permissionIds? }`  
**Response:** ข้อมูลบทบาทที่อัปเดตแล้ว

### 13. ลบบทบาท
```http
DELETE /auth/roles/:id
```
**วัตถุประสงค์:** ลบบทบาทออกจากระบบ (ไม่สามารถลบ system role)  
**Params:** `id` - Role ID  
**Response:** ข้อความยืนยันการลบ

### 14. เปิด/ปิดสถานะบทบาท
```http
PATCH /auth/roles/:id/toggle-status
```
**วัตถุประสงค์:** เปลี่ยนสถานะ `isSystem` ของบทบาท  
**Params:** `id` - Role ID  
**Response:** ข้อมูลบทบาทที่อัปเดตสถานะแล้ว

### 15. ดูผู้ใช้ในบทบาท
```http
GET /auth/roles/:id/users
```
**วัตถุประสงค์:** ดูรายการผู้ใช้ที่มีบทบาทนี้  
**Params:** `id` - Role ID  
**Response:** Array ของผู้ใช้ (ไม่รวม password)

---

## 🔑 Permission Management Endpoints (8)

### 16. ดูรายการสิทธิ์ทั้งหมด
```http
GET /auth/permissions
```
**วัตถุประสงค์:** ดึงรายการสิทธิ์ทั้งหมด  
**Response:** Array ของสิทธิ์

### 17. สร้างสิทธิ์ใหม่
```http
POST /auth/permissions
```
**วัตถุประสงค์:** สร้างสิทธิ์ใหม่  
**Body:** `{ code, name, description?, module? }`  
**Response:** ข้อมูลสิทธิ์ใหม่

### 18. ดูข้อมูลสิทธิ์เฉพาะ
```http
GET /auth/permissions/:id
```
**วัตถุประสงค์:** ดูรายละเอียดสิทธิ์พร้อมบทบาทที่มีสิทธิ์นี้  
**Params:** `id` - Permission ID  
**Response:** ข้อมูลสิทธิ์ครบถ้วน

### 19. แก้ไขข้อมูลสิทธิ์
```http
PUT /auth/permissions/:id
```
**วัตถุประสงค์:** อัปเดตข้อมูลสิทธิ์  
**Params:** `id` - Permission ID  
**Body:** `{ code?, name?, description?, module? }`  
**Response:** ข้อมูลสิทธิ์ที่อัปเดตแล้ว

### 20. ลบสิทธิ์
```http
DELETE /auth/permissions/:id
```
**วัตถุประสงค์:** ลบสิทธิ์ออกจากระบบ (ไม่สามารถลบสิทธิ์ที่กำลังใช้งาน)  
**Params:** `id` - Permission ID  
**Response:** ข้อความยืนยันการลบ

### 21. ดูบทบาทที่มีสิทธิ์นี้
```http
GET /auth/permissions/:id/roles
```
**วัตถุประสงค์:** ดูรายการบทบาทที่มีสิทธิ์นี้  
**Params:** `id` - Permission ID  
**Response:** Array ของบทบาท

### 22. ดูสิทธิ์ตาม Module
```http
GET /auth/permissions/modules/:module
```
**วัตถุประสงค์:** ดูรายการสิทธิ์ในโมดูลเฉพาะ  
**Params:** `module` - Module name  
**Response:** Array ของสิทธิ์ในโมดูลนั้น

### 23. ดูรายการ Modules ทั้งหมด
```http
GET /auth/modules
```
**วัตถุประสงค์:** ดูรายการโมดูลทั้งหมดที่มีในระบบ  
**Response:** Array ของชื่อโมดูล

---

## 🔗 Role-Permission Relationship Management (3)

### 24. กำหนดสิทธิ์ให้บทบาท (แทนที่ทั้งหมด)
```http
PUT /auth/roles/:id/permissions
```
**วัตถุประสงค์:** กำหนดสิทธิ์ทั้งหมดให้บทบาท (แทนที่สิทธิ์เดิม)  
**Params:** `id` - Role ID  
**Body:** `{ permissionIds: ["1", "2", "3"] }`  
**Response:** ข้อมูลบทบาทพร้อมสิทธิ์ใหม่

### 25. เพิ่มสิทธิ์ให้บทบาท
```http
POST /auth/roles/:roleId/permissions/:permissionId
```
**วัตถุประสงค์:** เพิ่มสิทธิ์เดี่ยวให้กับบทบาท  
**Params:** `roleId` - Role ID, `permissionId` - Permission ID  
**Response:** ข้อมูลบทบาทที่อัปเดตแล้ว

### 26. ลบสิทธิ์ออกจากบทบาท
```http
DELETE /auth/roles/:roleId/permissions/:permissionId
```
**วัตถุประสงค์:** ลบสิทธิ์เดี่ยวออกจากบทบาท  
**Params:** `roleId` - Role ID, `permissionId` - Permission ID  
**Response:** ข้อมูลบทบาทที่อัปเดตแล้ว

---

## 👤 User-Role Relationship Management (3)

### 27. กำหนดบทบาทให้ผู้ใช้ (แทนที่ทั้งหมด)
```http
PUT /auth/users/:id/roles
```
**วัตถุประสงค์:** กำหนดบทบาททั้งหมดให้ผู้ใช้ (แทนที่บทบาทเดิม)  
**Params:** `id` - User ID  
**Body:** `{ roleIds: ["1", "2"] }`  
**Response:** ข้อมูลผู้ใช้พร้อมบทบาทใหม่

### 28. เพิ่มบทบาทให้ผู้ใช้
```http
POST /auth/users/:userId/roles/:roleId
```
**วัตถุประสงค์:** เพิ่มบทบาทเดี่ยวให้กับผู้ใช้  
**Params:** `userId` - User ID, `roleId` - Role ID  
**Response:** ข้อมูลผู้ใช้ที่อัปเดตแล้ว

### 29. ลบบทบาทออกจากผู้ใช้
```http
DELETE /auth/users/:userId/roles/:roleId
```
**วัตถุประสงค์:** ลบบทบาทเดี่ยวออกจากผู้ใช้  
**Params:** `userId` - User ID, `roleId` - Role ID  
**Response:** ข้อมูลผู้ใช้ที่อัปเดตแล้ว

---

## 📊 Query & Utility Endpoints (3)

### 30. ดูโปรไฟล์ผู้ใช้ (สำหรับควบคุมเมนู)
```http
GET /auth/profile/:id
```
**วัตถุประสงค์:** ดูข้อมูลโปรไฟล์ผู้ใช้พร้อมบทบาทและสิทธิ์ (สำหรับควบคุมการแสดงเมนู)  
**Params:** `id` - User ID  
**Response:** `{ user, roles: ["ADMIN"], permissions: ["USER_CREATE", ...] }`

### 31. ดูผู้ใช้ที่มีบทบาทนี้
```http
GET /auth/roles/:id/users-with-role
```
**วัตถุประสงค์:** ดูรายการผู้ใช้ทั้งหมดที่มีบทบาทนี้  
**Params:** `id` - Role ID  
**Response:** Array ของผู้ใช้

### 32. ดูบทบาทที่มีสิทธิ์นี้
```http
GET /auth/permissions/:id/roles-with-permission
```
**วัตถุประสงค์:** ดูรายการบทบาททั้งหมดที่มีสิทธิ์นี้  
**Params:** `id` - Permission ID  
**Response:** Array ของบทบาท

---

## 🎯 Use Cases & Examples

### 🔐 Authentication Flow
```javascript
// 1. สมัครสมาชิก
POST /auth/register
{
  "username": "john_doe",
  "email": "john@example.com", 
  "password": "securePassword123",
  "firstName": "John",
  "lastName": "Doe"
}

// 2. เข้าสู่ระบบ
POST /auth/login
{
  "username": "john_doe",
  "password": "securePassword123"
}
```

### 👥 User Management Flow
```javascript
// 1. ดูรายการผู้ใช้
GET /auth/users

// 2. แก้ไขผู้ใช้
PUT /auth/users/1
{
  "firstName": "John Updated",
  "roleIds": ["1", "2"]
}

// 3. เปิด/ปิดผู้ใช้
PATCH /auth/users/1/toggle-status
```

### 🎭 Role & Permission Setup
```javascript
// 1. สร้างสิทธิ์
POST /auth/permissions
{
  "code": "USER_MANAGE",
  "name": "User Management",
  "module": "user-management"
}

// 2. สร้างบทบาท
POST /auth/roles
{
  "code": "ADMIN",
  "name": "Administrator",
  "permissionIds": ["1", "2", "3"]
}

// 3. กำหนดบทบาทให้ผู้ใช้
PUT /auth/users/1/roles
{
  "roleIds": ["1"]
}
```

### 🔗 Relationship Management
```javascript
// 1. เพิ่มสิทธิ์ให้บทบาท
POST /auth/roles/1/permissions/4

// 2. เพิ่มบทบาทให้ผู้ใช้
POST /auth/users/1/roles/2

// 3. ตรวจสอบสิทธิ์สำหรับควบคุมเมนู
GET /auth/profile/1
```

---

## 🔒 Security Features

### ✅ Password Security
- **bcrypt hashing** สำหรับเข้ารหัสรหัสผ่าน
- **Password exclusion** ไม่ส่ง passwordHash ใน response
- **Minimum length validation** รหัสผ่านขั้นต่ำ 6 ตัวอักษร

### ✅ Data Validation
- **DTO validation** ใช้ class-validator
- **Unique constraints** username, email, role code, permission code
- **Required field validation** ตรวจสอบข้อมูลจำเป็น

### ✅ Business Logic Protection
- **System role protection** ไม่สามารถลบ system roles
- **Permission usage check** ไม่สามารถลบสิทธิ์ที่กำลังใช้งาน
- **Duplicate prevention** ป้องกันการกำหนดสิทธิ์/บทบาทซ้ำ

### ✅ Database Security
- **TypeORM ORM** ป้องกัน SQL Injection
- **Transaction support** การเปลี่ยนแปลงแบบ atomic
- **Relationship integrity** รักษาความสมบูรณ์ของข้อมูล

---

## 📈 Performance Features

### ⚡ Optimized Queries
- **Eager loading** โหลดข้อมูลที่เกี่ยวข้องในครั้งเดียว
- **Selective fields** เลือกเฉพาะฟิลด์ที่จำเป็น
- **Index optimization** ใช้ index สำหรับการค้นหา

### ⚡ Caching Ready
- **Stateless design** พร้อมสำหรับ caching
- **Consistent response format** รูปแบบ response ที่สม่ำเสมอ
- **Minimal data transfer** ส่งเฉพาะข้อมูลที่จำเป็น

---

## 🛠️ Development Tools

### 📝 API Testing
```bash
# ใช้ curl หรือ Postman ทดสอบ endpoints
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"password"}'
```

### 🔍 Error Handling
- **HTTP Status Codes** ใช้ status codes ที่เหมาะสม
- **Descriptive Messages** ข้อความ error ที่ชัดเจน
- **Validation Errors** รายละเอียด validation errors

### 📊 Monitoring Ready
- **Structured Logging** log ที่มีโครงสร้าง
- **Error Tracking** ติดตาม errors
- **Performance Metrics** วัดประสิทธิภาพ

---

## 🎉 Summary

**ระบบ Authentication & Authorization ที่สมบูรณ์แบบ:**

- ✅ **32 Endpoints** ครอบคลุมทุกการใช้งาน
- ✅ **RBAC System** Role-Based Access Control
- ✅ **Security First** ความปลอดภัยระดับสูง  
- ✅ **Developer Friendly** ง่ายต่อการใช้งานและพัฒนา
- ✅ **Production Ready** พร้อมใช้งานจริง
- ✅ **Scalable Architecture** สถาปัตยกรรมที่ขยายได้

**เหมาะสำหรับ:**
- 🏢 Enterprise Applications
- 🌐 Web Applications  
- 📱 Mobile API Backend
- 🔐 Admin Dashboards
- 👥 Multi-tenant Systems